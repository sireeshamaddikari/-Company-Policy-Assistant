# ---------------------------------------------------------------------------
# Company RAG Chatbot — backend dependencies
# Pinned for reproducible, free-tier installs.
# ---------------------------------------------------------------------------

# --- Web framework / server ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.18          # file uploads (multipart/form-data)

# --- Configuration / validation ---
pydantic==2.10.4
pydantic-settings==2.7.1
python-dotenv==1.0.1

# --- Persistence (SQLite via SQLAlchemy ORM) ---
SQLAlchemy==2.0.36

# --- Vector store (local, free) ---
faiss-cpu==1.9.0.post1
# Embeddings are produced by the hosted Mistral API (see app/rag/embeddings.py),
# so no local model / PyTorch is installed — keeps the deploy image small.

# --- Document loading & splitting (LangChain: loaders/splitters only) ---
langchain==0.3.13
langchain-community==0.3.13
langchain-text-splitters==0.3.4
PyPDF2==3.0.1
python-docx==1.1.2

# --- LLM API client (Mistral via REST) ---
httpx==0.28.1

# --- Testing (optional, used in later steps) ---
pytest==8.3.4
