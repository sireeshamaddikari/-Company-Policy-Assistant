"""Services package: RAG orchestration, embeddings, retrieval and LLM access.

Each concern (embedding, vector store, retrieval, generation) will live in its
own module here. No logic is implemented in this step.
"""
