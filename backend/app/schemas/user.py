"""User / auth API schemas (stub) — reserved for future authentication.

Planned: UserCreate (email, password), UserRead (id, email, is_active),
Token (access_token, token_type).

No models are defined in this step.
"""
