"""Citation API schema (stub).

Planned: CitationRead (document_id, filename, chunk_index, snippet, score)
embedded in assistant answers so the UI can render sources.

No models are defined in this step.
"""
