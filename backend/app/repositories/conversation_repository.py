"""Conversation repository (stub).

Persistence for chat conversations: create, list (most-recent first), get by id,
delete. Scoped per user once auth is added.

No logic is implemented in this step.
"""
